CREATE TABLE tenants(id UUID PRIMARY KEY,name VARCHAR(150) NOT NULL,slug VARCHAR(80) NOT NULL UNIQUE,contact_email VARCHAR(254) NOT NULL,status VARCHAR(20) NOT NULL,created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP,updated_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP);
