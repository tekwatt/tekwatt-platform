package com.tekwatt.notification.repository;import com.tekwatt.notification.entity.Notification;import java.util.*;import org.springframework.data.jpa.repository.JpaRepository;public interface NotificationRepository extends JpaRepository<Notification,UUID>{Optional<Notification>findByIdempotencyKey(String key);List<Notification>findAllByTenantIdOrderByCreatedAtDesc(UUID tenantId);}
